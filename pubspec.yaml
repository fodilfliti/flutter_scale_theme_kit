name: flutter_scale_theme_kit
description: >
  Look companion for Flutter: semantic colors, light/dark tokens, and Material
  ThemeData. Pair with flutter_scale_kit for size. Independent — no scale_kit dependency.
version: 1.0.1
homepage: https://github.com/fodilfliti/flutter_scale_theme_kit
repository: https://github.com/fodilfliti/flutter_scale_theme_kit
issue_tracker: https://github.com/fodilfliti/flutter_scale_theme_kit/issues
documentation: https://github.com/fodilfliti/flutter_scale_theme_kit#readme

environment:
  sdk: ^3.7.2
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

executables:
  generate: generate

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - theme
  - design-system
  - material
  - tokens
  - colors

flutter:
