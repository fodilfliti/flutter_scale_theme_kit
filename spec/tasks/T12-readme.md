# T12 — README + CHANGELOG

## Goal

User docs match the new API. Kill the seed/`provider`/`ScaleThemeConfig` story.

## Read first

- [../package.md](../package.md)
- [../companion.md](../companion.md)

## Files

- `README.md`
- `CHANGELOG.md` (0.1.0 or 0.0.1 notes)

## Steps

1. What it is / is not; levels 1–2; `STTheme` + `MaterialApp`; `context.st`; companion snippet; independence.
2. No `ScaleThemeController`, no `seedColor` as the hero API.
3. CHANGELOG: rebuild as look companion.

## Tests

None (docs).

## Done when

README examples compile conceptually with public types.

## Do not

Add a consumer `skills/` tree in this task (later, when API is stable).
